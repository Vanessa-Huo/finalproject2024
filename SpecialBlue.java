import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class SpecialBlue here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class SpecialBlue extends Blueberry
{
    /**
     * Act - do whatever the SpecialBlue wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public SpecialBlue(){
        image = new GreenfootImage("button-blue.png");
        image.scale(56,56);
        setImage(image);
    }
}
